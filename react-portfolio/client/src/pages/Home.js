import React from "react";
import { Card, Modal } from "../components/Card";

const Home = () => {
  return (
    <div className="container">
      <Card />
      <Modal />
    </div>
  );
};

export default Home;
